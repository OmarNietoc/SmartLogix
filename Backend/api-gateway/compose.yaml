services:
  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-gateway
    ports:
      - '8080:8080'
    environment:
      - SERVER_PORT=8080
      - EUREKA_URI=http://host.docker.internal:8761/eureka/
