# eureka-server

Servidor Netflix Eureka para discovery de microservicios.

- Puerto: `8761`
- Consola: `http://localhost:8761`
- Variables: `SERVER_PORT`

Comandos:

```bash
mvn spring-boot:run
mvn clean test
mvn clean verify
```

Cobertura: `target/site/jacoco/index.html`.
