server:
  port: ${SERVER_PORT:8081}

spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: ms-inventary
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/smartlogix_inventario}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:drhiaishna16}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    enabled: false # Se habilitará en la Fase 3
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}