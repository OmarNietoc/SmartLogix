package com.smartlogix.shipping.event;

// Replaced by ReservationConfirmedConsumer — shipping now creates shipments
// only after inventory confirms stock (order.reservation.confirmed event).
public class OrderCreatedConsumer {}
